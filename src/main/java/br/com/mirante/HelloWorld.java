package br.com.mirante;

import java.sql.SQLException;

public class HelloWorld {

	public static void main(String[] args) throws SQLException {

		/*
		 * EntityManagerFactory fabrica =
		 * Persistence.createEntityManagerFactory("default");
		 * 
		 * EntityManager entityManager = fabrica.createEntityManager();
		 * entityManager.getTransaction().begin(); Aluno a =
		 * entityManager.find(Aluno.class, "Novo aluno"); Aluno a19 = new Aluno();
		 * List<Aluno> alunos =
		 * entityManager.createQuery("SELECT a FROM Aluno a").getResultList();
		 * entityManager.remove(a); alunos.forEach(aluno ->
		 * System.out.println(aluno.nome)); entityManager.getTransaction().commit(); //
		 * entityManager.persist(new Orcamento());
		 */
	}
}
